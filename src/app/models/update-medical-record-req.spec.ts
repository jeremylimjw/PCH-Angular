import { UpdateMedicalRecordReq } from './update-medical-record-req';

describe('UpdateMedicalRecordReq', () => {
  it('should create an instance', () => {
    expect(new UpdateMedicalRecordReq()).toBeTruthy();
  });
});
